export function setupTracing() {
  // OpenTelemetry setup - disabled for now due to dependency conflicts
  // Can be re-enabled later when needed
  console.log('OpenTelemetry tracing disabled - set ENABLE_OTEL=true to enable');
}